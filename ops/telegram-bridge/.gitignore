# Local runtime state — see pendingStore.cjs. Never commit real
# message/interaction mappings or secret values.
.pending-store.json

# Secrets: keep the .env alongside the code but never commit it.
.env
.env.*
!.env.example
