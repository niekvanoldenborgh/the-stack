{
  "name": "the-stack-server",
  "version": "0.1.0",
  "private": true,
  "description": "Backend/account-schema tooling for the-stack. Not bundled into the Expo app — see server/README.md.",
  "type": "module",
  "scripts": {
    "migrate": "node scripts/migrate.mjs",
    "start": "node src/server.mjs",
    "test": "node --test \"test/**/*.test.mjs\""
  },
  "dependencies": {
    "argon2": "^0.45.1",
    "express": "^5.2.1",
    "jsonwebtoken": "^9.0.3",
    "mysql2": "^3.23.3"
  }
}
